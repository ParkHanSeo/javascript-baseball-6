import { Console } from "@woowacourse/mission-utils";
import { OUTPUT_MESSAGE } from '../contants/Message.js'

const OuputView = {
    outputBaseballStart() {
        Console.print(OUTPUT_MESSAGE.BASEBALL_START);
    }
}

export default OuputView